import React from 'react';
import c from 'classnames';

const App = () => {
  const className = c('App');

  return (
    <div className={className}>
      Hello world.
    </div>
  );
};

export default App;
